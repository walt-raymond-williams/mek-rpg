# Active Campaign Pointer

Use this file before play to decide which campaign save folder to load.

Active campaign: campaigns/sharpes-strikers/

Default template: `campaigns/_template/`

Recent playtest save: `campaigns/playtest-galatea-dropship/`
Current table-canon save: `campaigns/isekai-atlas-field/`
Current MekHQ-linked playtest save: `campaigns/the-learning-ropes/`
Current active save: `campaigns/sharpes-strikers/`

## Selection Rules

- Load exactly one campaign folder for normal play.
- Do not mix playtest-only facts into table canon unless the user explicitly promotes them.
- If the user asks to resume play and no active campaign is selected, ask which campaign to load.
- Shared rules, indexes, and GM procedures are global. PCs, NPCs, assets, locations, relationships, missions, hooks, and session notes belong inside the active campaign folder.

## Status Notes

- `campaigns/playtest-galatea-dropship/` contains the issue `#12` Galatea DropShip purchase playtest. It is inactive and playtest-only.
- `campaigns/isekai-atlas-field/` is the first table-canon campaign save. The opening premise is an isekai-style arrival in the BattleTech universe: Walter and his son appear in a field near an unattended Atlas with its cockpit open.
- `campaigns/the-learning-ropes/` is the issue `#97` MekHQ-linked blind playtest save, bootstrapped from the loaded read-only MekHQ campaign `The Learning Ropes` and active while that checkpoint is being run.
- `campaigns/sharpes-strikers/` is the active campaign save for Sharpe's Strikers. It starts clean from the standard campaign template and should not import PCs, NPCs, assets, missions, hooks, or session notes from prior campaigns unless the user explicitly asks to promote a fact.
